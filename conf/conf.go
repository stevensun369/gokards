package conf

var(
	// DSN is the connection string to the database
	DSN = "user=postgres password=StevenS369@postgres dbname=kards_dev port=5432 sslmode=disable TimeZone=Europe/Bucharest"
	// ViewsFolder represents the views folder on disk
	ViewsFolder = "./views"
	// StaticFolder represents the static folder on disk
	StaticFolder = "./static"
	// MediaFolder represents the media folder on disk
	MediaFolder = "./media"
)